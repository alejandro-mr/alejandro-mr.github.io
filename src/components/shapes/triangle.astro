---
const { variant } = Astro.props;
const triangleStyle = `inline-block border-l-[20px] border-l-transparent border-r-[20px] border-r-transparent border-b-[40px] border-red-500 cursor-pointer select-none`;
---

<span class={triangleStyle} />